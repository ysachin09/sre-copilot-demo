spring:
  application:
    name: inventory-api

server:
  port: 8083

log:
  dir: ${LOG_DIR:logs}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
